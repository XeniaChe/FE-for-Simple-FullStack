{
  "name": "jsfe-withbe-app",
  "version": "1.0.0",
  "description": "Simple app with FE on JS and BE on NodeJS(Express)",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./build"
  },
  "author": "Xenia Cserkun",
  "license": "ISC",
  "dependencies": {
    "core-js": "^3.7.0",
    "regenerator-runtime": "^0.13.7"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "parcel": "^2.0.0-beta.1"
  }
}
